# HumanDraft Obsidian Starter

This starter plugin sends selected text to the local HumanDraft Web UI server:

```bash
npx humandraft web
```

Then run `HumanDraft: Generate brief from selection` in Obsidian.

The plugin inserts the generated brief as JSON so it can be edited before drafting.
