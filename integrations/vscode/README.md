# HumanDraft VS Code Starter

This is a thin VS Code extension starter for HumanDraft.

It shells out to the public CLI:

```bash
npx humandraft brief "one-line demand"
npx humandraft write "one-line demand"
npx humandraft web
```

The extension is intentionally small so the CLI remains the source of truth.
